{
  "name": "anapath-back",
  "version": "1.0.0",
  "description": "Backend API for Anapath (anatomopathology lab)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test tests/*.test.js",
    "db:schema": "psql -d anapath -f src/db/schema.sql",
    "db:seed": "psql -d anapath -f src/db/seed.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
